.: Test.Main

-> Math.add
=> Math.(add sub div mul)

Bool :- True | False
    
MyObject :-
    // Define x with a type
    $ x: i32
    $ y: i32
        

test :.
    add x y : i64 -> i64 -> i64 :=
        ^ x + y

    $ foo : !i32  // Constant
    $ bar := 2    // Mutable

    add_nums : void :=
        // no type inference yet
        x : i64 <- add 1 2
        MyObject 17 19
        print x