add x y : i64 -> i64 -> i64 :=
  ^ x + y
    
//foo : i64 <- 1

add_nums : void :=
  // no type inference yet
  x : i64 <- add 1 2
  print x
  // no return statement, compiler should generate a blank one.
  