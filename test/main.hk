add x y
    : int -> int -> int :=
{
    return x + y;
}

@ main;
run
    : void :=
{
    // Declarations are implicit
    // let x : int;re
    
    // no type inference yet
    x : int <- add 1 2;
    
    print x;
}