.: Test.Main

-> Math.add
=> Math.(add sub div mul)

Bool :- True | False
    
MyObject :-
    $ x: i32
      y: i32
    
    add : i64 := // Aligned indent, wraps statements in floating indents when new lines are on the same column, closes when indent level is reduced. 
        ^ x + y

test :.
    $ bar = 2     // Mutable

    foo: void :=
        $ obj = MyObject 17 19
        $ x = obj.add
        print x