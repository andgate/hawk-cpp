< main -- name the module main, for classic sake

x = 1
> vector -- import vector


-[
  This is a multiline comment
]-

//* This is a document comment!

-|
 This is a multi-line document comment!
|-


-|
  dbl Returns double the value.
  x   Value to double.
  A pure function,
  is automatically inlined and optimized.
 |-
dbl@ public       -- Settings declaration
dbl: Int @ const
  -> Int          -- Type declaration
dbl x =           -- Expression declaration
  x + x           -- Single expression statement

-|| @sqr Returns the value squared.
   @x   Value to square.
   Another very nice, pure function.
  |-
sqr@ public inline -- the inline is optional, and will be inferred (eventually!).
sqr: Int -> Int
sqr x = x * x


main@ program="test" public
main: ()
main =
  y = dbl (sqr x)
  // Print the result
  // (disable for easier development now)
  //print y
  return
