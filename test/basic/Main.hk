module Main

dbl@ public
dbl: Int -> Int
dbl x =
  x + x

sqr@ public
sqr: Int -> Int
sqr x = x * x

main@ program="test" public
main: ()
main =
  y = dbl (sqr x)
  -- Print the result
  -- (disable for easier development now)
  --print y
  return
